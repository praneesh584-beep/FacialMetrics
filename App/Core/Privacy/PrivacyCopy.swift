import Foundation

enum PrivacyCopy {
    static let scanConsent = "FaceMetric processes scan samples on this device. No scan is uploaded automatically, and exports happen only when you choose them."
    static let exportWarning = "Diagnostic exports may include device and scanner state, but they should not include RGB face images, credentials, account data, or raw mesh data by default."
}
